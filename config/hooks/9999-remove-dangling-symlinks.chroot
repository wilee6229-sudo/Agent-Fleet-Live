#!/bin/sh
set -e
# Remove dangling symlinks that cause lb_chroot_hacks to fail
# Runs as the LAST hook (9999) after all packages are installed
# NOTE: hooks run INSIDE the chroot (cwd=/), so paths are relative to the chroot root
rm -f boot/initrd.img boot/initrd.img.old 2>/dev/null || true
